export const sendNotification = async (userId: string, title: string, body: string) => {
  // Placeholder logic since the actual implementation wasn't provided yet
  console.log(`Notification sent to ${userId} - ${title}: ${body}`);
};
